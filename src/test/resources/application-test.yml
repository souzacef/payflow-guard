spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payflowguard
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

app:
  webhooks:
    payment-status-url: "https://webhook.site/test-placeholder"